export type ElementPropertyOptionBadgeColor = "blue" | "cyan" | "fuchsia" | "green" | "grey" | "light-grey" | "orange" | "pink" | "purple" | "red" | "teal" | "yellow" | "custom";
export type ElementPropertyOptionRemoteModel = {
    id: string;
    name: string;
    backgroundColor: {
        value: string;
    };
};
export type ElementPropertyOptionCreationModel = ElementPropertyOptionRemoteModel;
export type ElementPropertyOptionUpdateModel = ElementPropertyOptionRemoteModel;
export declare class ElementPropertyOption {
    id: string;
    name: string;
    backgroundColor: string | null;
    constructor(model: ElementPropertyOptionRemoteModel);
    /** Constructs directly manipulable update object for user profile */
    toUpdateObject(): ElementPropertyOptionUpdateModel;
    /** Returns background color represented as badge color. If badge color is different than what badge defines as base, return custom. Note: Only temporary implementation until BE supports it properly */
    asBadgeColor(): ElementPropertyOptionBadgeColor;
    /** Sets background color based on badge identifier color. Note: Only temporary implementation until BE supports it properly */
    setFromBadgeColor(color: ElementPropertyOptionBadgeColor): void;
}
